version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14

    environment:
      MY_VARIABLE: my_value

    steps:
      - checkout
      - run: npm install
      - run: npm start
      # - run: npm test

workflows:
  build_and_test:
    jobs:
      - build
      - test